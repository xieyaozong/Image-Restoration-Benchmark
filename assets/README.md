# Assets

This folder contains publishable demo assets.

- `samples/`: open-source natural input images (see `samples/SOURCES.md` for credits)
- `example_outputs/`: regenerated benchmark outputs used by the README

Private or licensed datasets should stay under `data/raw/`, which is ignored by git.

