# Data

Place private or external datasets here when running local experiments.

Recommended layout:

```text
data/
  raw/
  processed/
```

`data/raw/` is ignored by git. Do not publish private, licensed, or unclear-source images.

